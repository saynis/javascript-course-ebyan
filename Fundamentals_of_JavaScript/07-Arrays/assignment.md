# Assignment 08 — Arrays 📦

> **How to submit:** Create a file called `answers.js`

---

## Task 1 — Favorite Movies 🎬

Create an array called `movies` with 5 movie names.

Then:

- print the whole array
- print the first movie
- print the last movie

---

## Task 2 — Student List 👨‍🎓

Create an array called `students` with 4 student names.

Then:

- replace the second student with a new name
- print the updated array
- print the length of the array

---

## Task 3 — Add and Remove ➕

Create this array:

```js
let cities = ["Mogadishu", "Hargeisa", "Jigjiga"]
```

Then:

- add one city to the end 
- add one city to the beginning 
- remove the last city using 
- print the final array

---

## Task 4 — Search in Array 🔍

Create an array called `languages` with at least 5 programming languages.

Then:

- check if `"JavaScript"`
- find the index of `"Python"` 
- print both results

---

## Task 5 — Loop Through Array 🔁

Create an array called `numbers` with these values:

```js
[10, 20, 30, 40, 50]
```

Use a `for ...of` loop to:

- print every number
- print the total of all numbers

---

## Task 6 — Shopping Cart 🛒

Create an array called `cart` with 3 products.

Then:

- print the number of items in the cart
- add 2 new products
- remove 1 product
- print the updated cart

---

## 🌟 Bonus Task

Create an array called `scores`:

```js
[55, 70, 88, 95, 60]
```

Use a loop to:

- print every score
- print only scores greater than 70

---

# ✅ Checklist

- [ ] I used arrays correctly
- [ ] I used array indexes
- [ ] I used loops with arrays
- [ ] My code runs without errors